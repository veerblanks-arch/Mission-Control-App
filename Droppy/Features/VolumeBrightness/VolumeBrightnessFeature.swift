import Foundation

struct VolumeBrightnessFeature {
    static let phase = 1
    static let status = "Scrapped for MVP; native macOS volume and brightness controls are better than a partial clone."
}
